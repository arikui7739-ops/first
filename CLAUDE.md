# 花王 石狩配送センター ロケーション最適化VBAシステム

Excel(.xlsm)ベースの倉庫ロケーション最適化・KPI管理システム。石狩配送センターの棚割り最適化を、実績ピッキングデータ（WMSログ）から自動分析し、交換提案・入替指示書・KPIダッシュボードを生成する。VBAモジュールのソースは `vba/` 配下にある。

## このプロジェクトで作業する際の注意

- VBAモジュール(.bas)をユーザーに納品する前に、必ず **Shift-JIS(CP932)に変換**すること。VBEはUTF-8のままインポートすると文字化けする。
- 変換後は必ずCP932→デコードして元のUTF-8内容と往復一致することを確認してから渡す（`errors='replace'`で潜在的な文字化けを検知できる）。
- VBAの1文あたりの行連結（`& _`）は**24行まで**という制限がある。超えると「この行の連結が多すぎます」エラーになるため、長い文字列連結は複数の変数に分けてから結合すること。
- VBAには`Round()`関数がない。`Application.WorksheetFunction.Round`を使うこと。
- 異なる型の変数を関数の引数に渡す際は`ByVal`を付けないと「ByRef引数の型が一致しません」エラーになる。
- Excelの行の高さ（RowHeight）は行全体（全列）に影響する。説明文などの折り返しテキストとデータテーブルが同じ行範囲を共有する場合、大きなRowHeightを設定するとデータ行まで見づらくなる。
- KPI記録シートは列構成を変更するたびに、ユーザー側で既存シートを削除してから再実行してもらう必要がある（過去データは新しい列位置に自動移行されない）。

## 全体構成

### エリア構成（業務前提）
| エリア | 号機 | 方式 | 特徴 |
|---|---|---|---|
| AB | 1～46号機 | ケースフローラック・デジタルピッキング | 高頻度品、生産性高い。1&2号機・3&4号機…45&46号機が対面ペア（ゾーン1～23） |
| C | 51～68号機（59,60は欠番） | 中密度棚 | 中頻度品、生産性中程度 |
| X | 70～98号機 | 低頻度棚 | 最低頻度品 |

- 1・2号機の6～14列は「中量棚」の例外区画（通常のAB集計から除外）
- Module3のスワップ対象からは1～4号機を丸ごと除外（`IsExcludedSlot3`）
- 「編成」= 6件の連続オーダー（H行）をひとまとめにした単位（AB導線分析用）
- 月間約2000SKU出荷、棚入替はほぼ毎日実施だが完璧な追従は困難という前提
- 号機回数比シートの許容乖離：AB=0.2pt、C+X合算=2%（X単独は1%目標）

### WMSピッキングログのファイル形式
- `B`行：先頭B+8桁日付（YYYYMMDD）＝実績日
- `H`行：オーダー/トート区切り。`H99999...`は各ロケの在庫数サマリー行（実オーダーではないため除外）
- `E`行：ピッキング実績。9桁サブブロック（号機2桁+段2桁+列2桁+数量3桁）が13文字ストライドで1行に最大3件。埋まっていないスロットは「号機00」のパディングになるため、集計時は`mach > 0`で除外する必要がある
- `END0000083`：ファイル末尾トレーラー
- 商品コードはファイルに存在しないため、ロケーション自体をアイテムキーとして扱う（品名・商品コードはCFシートから引く）

## VBAモジュール一覧（`vba/`）

### Module1: `SwapLocationsByCorrelationFast_Fix`
Cエリア実績ベースの同時ピッキング交換指示書作成。複数ファイル選択可、B行から実績日、H99999除外、商品コード数値化。完了後、平均無駄歩行スコアを`Module7.LogModule1Score`でKPI記録。

### Module3: `OptimizeABFormationFlow`
AB間口の対面ピッキング動線最適化（実オーダー・編成単位）。H行6件＝1編成、同一ゾーン共起・対面共起をペアとして記録し、ゾーン&サイド制約付き2パス探索でスワップ候補を選定。奇数偶数バランスを`|差|≦0.001`まで収束させる。完了後、以下のKPIを`Module7.LogFormationScore`で記録：

- **AB号機使用比率スコア**：`sumAbsDiff = Σ|実績比率－目標比率|`（号機5～46）、`abRatioScore = Max(0, 100×(1－sumAbsDiff÷0.6))`（0.6以上で0点）
- **AB占有率スコア**：理論比率＝実績ファイル全体を回数順に並べた上位900ロケーションの回数比率、実績比率＝AB間口(1～46号機、6～14列除く)の回数比率。`occDeviation ≦1%→100点／≧6%→0点`、間は比例配分
- **奇偶バランス(交換前/交換後)**：交換前＝スワップ提案前の実績ヒットで計算（日々の実態）、交換後＝提案反映後の見込み値（総合スコアに使用。構造上高得点に収束しやすい）
- **対面同時ヒットスコア**：`100×(1－対面ペア数÷全ペア数)`

### Module4: `CreatePredictionLocationInstruction`（出力シート：ゾーンバランス最適）
号機回数比の乖離ベースでAB間口の昇格・降格を予測提案。降格カットオフは全アイテム中の予測回数900位。※予測データのためKPI記録対象外。

### Module5: `GenerateLocationInstructions`（出力シート：Cバラ動線最適化）
Cエリア空きロケーションへの入替指示書。号機重み・列重みによるペナルティスコア。移動先の現在の入居品を表示。※予測データのためKPI記録対象外。

### Module6: `CreateControlPanel`（出力シート：操作パネル）
Module1・Module3・Module4・Module5を実行できるフォームボタンを設置。

### Module7: KPI記録シート管理
実績データのみを対象にKPIを記録・可視化する複合スコアシート。

**列構成**：A実績日(キー)／B曜日／C AB号機使用比率スコア／D AB占有率スコア／E理論比率／F実績比率／G奇偶バランス(交換前)／H奇偶バランス(交換後)／I対面同時ヒット／J Cバラ無駄歩行(Module1)／K総合スコア／L評価／M更新時刻／O以降=説明文。見出しは2行折り返し＋列幅固定でコンパクト表示。

**総合スコア**：`AB号機使用比率×40 + AB占有率×15 + 奇偶バランス(交換後)×25 + 対面同時ヒット×20 + Cバラ無駄歩行×15` の加重平均（未実行の指標は重みを再正規化）。評価：70点以上=◎良好(緑)／40～69点=○普通(黄)／40点未満=×要対策(赤)。

## 今後の調整予定・申し送り事項

- Cバラ無駄歩行スコアの暫定式（÷20の部分）は仮基準。数週間分の実績が溜まったら調整すること
- 各種スコアのしきい値（AB占有率1%/6%、AB号機使用比率0.6等）は今後の実データ蓄積により見直しの余地あり
- 元のExcelワークブック本体（.xlsm）はこのリポジトリには含まれていない（ユーザーからのアップロードファイルのみ）
